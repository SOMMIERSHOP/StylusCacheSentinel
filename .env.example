ARB_RPC_URL=https://arb1.arbitrum.io/rpc
DB_PATH=./sentinel.db

# Wallet for the sentinel automation layer (M3/M4).
# Required only for `sentinel run --live` and `sentinel bid --yes`.
# 32-byte hex private key (with or without 0x prefix). KEEP THIS SECRET.
# SENTINEL_PRIVATE_KEY=0x...
